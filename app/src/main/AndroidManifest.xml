<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aloogn.demo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".fragment.MainActivity" />
        <activity android:name=".layout.LoginActivity" />
        <activity android:name=".motion.MotionActivity" />
        <activity android:name=".mvp.TestActivity" />
        <activity android:name=".mvp.base.view.BaseActivity" />
        <activity android:name=".animation.PropertyActivity"></activity>
        <activity android:name=".jni.JNIActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>